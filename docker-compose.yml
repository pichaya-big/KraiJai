services:
  client:
    image: ghcr.io/pichaya-big/kraijai-client:latest
    container_name: kraijai-client
    restart: always
    ports:
      - "3000"
    networks:
      - nginx-proxy-net

networks:
  nginx-proxy-net:
    external: true